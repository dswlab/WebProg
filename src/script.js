/**
 * ====================================
 * 기말고사 실습: 이벤트 관리 앱
 * 학생이 직접 작성하는 JavaScript
 * ====================================
 */

// ========================================================
// [3단계 문제] DOM 요소 선택 + 이벤트 리스너 (10점)
// ========================================================
//
// 작성해야 할 내용:
// 1. DOM 요소 선택 (5점):
//    - document.getElementById('eventForm')
//    - document.getElementById('title')
//    - document.getElementById('date')
//    - document.getElementById('description')
//    - document.getElementById('eventsList')
//    - document.getElementById('errorMessage')
//
// 2. 폼 제출 이벤트 리스너 (5점):
//    - eventForm.addEventListener('submit', 함수)
//    - event.preventDefault() 호출
//    - 입력 값 가져오기 (trim() 사용)
//    - validateForm()으로 유효성 검사 (다음 단계에서 작성)
//    - 유효하면 addEventCard() 호출 (다음 단계에서 작성)
//    - eventForm.reset()으로 폼 초기화
//
// 완성 테스트: 브라우저 콘솔에서 DOM 요소들이 올바르게 선택되는지 확인
// 배점: DOM 선택 5점, 이벤트 리스너 5점
// ========================================================



// ========================================================
// [4단계 문제] 유효성 검사 + 에러 처리 (8점)
// ========================================================
//
// 작성해야 할 내용:
// 1. validateForm(title, date) 함수 (5점):
//    - title이 비어있으면 showError('제목을 입력해주세요.') 후 false 반환
//    - date가 비어있으면 showError('날짜를 선택해주세요.') 후 false 반환
//    - 유효하면 hideError() 호출 후 true 반환
//
// 2. showError(message) 함수 (1.5점):
//
// 3. hideError() 함수 (1.5점):
//
// 완성 테스트:
//    - 빈 제목으로 제출 시 "제목을 입력해주세요." 메시지 표시
//    - 빈 날짜로 제출 시 "날짜를 선택해주세요." 메시지 표시
//    - 에러 메시지가 빨간색 박스에 흔들리는 애니메이션으로 표시되는지 확인
// ========================================================



// ========================================================
// [5단계 문제] 카드 생성 + 날짜 변환 (10점)
// ========================================================
//
// 작성해야 할 내용:
// 1. addEventCard(title, date, description) 함수 (10점):

    // 빈 메시지 제거

    // 이벤트 카드 생성 (createElement 사용)

    // 카드 헤더 생성

    // 헤더에 자식 요소 추가 (appendChild 사용)

    // 설명 생성

    // 카드에 모든 요소 추가

    // 목록에 카드 추가

    // 성공 피드백

// 2. formatDate(dateString) 함수 (제공):
//    - new Date(dateString)로 Date 객체 생성
//    - getFullYear(), getMonth()+1, getDate()로 연/월/일 추출
//    - "YYYY년 MM월 DD일" 형식으로 반환
//    - getMonth()는 0부터 시작하므로 +1 필요
// ========================================================

function formatDate(dateString) {
    const date = new Date(dateString);
    const year = date.getFullYear();
    const month = date.getMonth() + 1;
    const day = date.getDate();
    return `${year}년 ${month}월 ${day}일`;
}

// ========================================================
// [7단계 문제] 이벤트 위임 + 삭제/수정 기능 (16점)
// ========================================================
//
// 작성해야 할 내용:
// 1. 메모리 데이터 관리를 위한 전역 변수 (2점):
//    - let events = []
//    - let nextId = 1
//
// 2. addEventCard 함수 수정 (4점):
//    - 카드 식별자 추가
//    - 이벤트 배열에 내용 추가
//    - 제어 버튼 추가
//
// 3. 이벤트 위임 제어 로직 구현 (5점):
//
// 4. deleteEvent(id, cardElement) 함수 (2.5점):
//
// 5. editEvent(id) 함수 (2.5점):
//
// 완성 테스트:
//    - 카드에 수정/삭제 버튼이 표시되는지 확인
//    - 삭제 버튼을 클릭하면 카드가 사라지는지 확인
//    - 수정 버튼을 클릭하면 폼에 내용이 채워지는지 확인
//    - 마지막 카드를 삭제하면 "등록된 이벤트가 없습니다."가 다시 표시되는지 확인
// 배점: 전역 변수 2점, addEventCard 수정 4점, 이벤트 위임 5점, deleteEvent 2.5점, editEvent 2.5점
// ========================================================





// ========================================================
// 도움말: 개발자 도구 사용법
// ========================================================
//
// 디버깅을 위해 console.log()를 적극 활용하세요:
// - console.log('DOM 요소:', eventForm)
// - console.log('입력 값:', title, date, description)
// - console.log('유효성 검사 결과:', isValid)
// - console.log('생성된 카드:', card)
// - console.log('이벤트 배열:', events)
//
// 개발자 도구 단축키:
// - F12 또는 Ctrl+Shift+I (Windows/Linux)
// - Cmd+Option+I (Mac)
// - Console 탭에서 console.log() 출력 확인
// - Elements 탭에서 DOM 구조 확인
//
// ========================================================
